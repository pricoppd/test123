resource "null_resource" "delay" {
  provisioner "local-exec" {
    command = "sleep 60"  # Sleep for 60 seconds
  }

  triggers = {
    always_run = "${timestamp()}"
  }
}

String random_pwd = java.util.Base64.getEncoder().encodeToString(new byte[12]).substring(0, 12);
