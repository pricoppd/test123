apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-port-9090
  namespace: gatekeeper
spec:
  podSelector:
    matchLabels:
      app: gatekeeper-audit
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector: {} # This allows traffic from any pod. Adjust this if necessary.
    ports:
    - protocol: TCP
      port: 9090
